"""Phase 4: LangGraph state machine for the Sales & Growth Copilot.

Pipeline: intent_classifier -> data_retriever -> analyzer -> response_synthesizer.
The agent only narrates data retrieved through tools/ — it never fabricates numbers.
"""
